# language setting
language: ruby

# version numbers
rvm:
 - 2.1.0

before_script:
 - psql -c 'create database acroparty_test;' -U matt

before_install:
  - sudo apt-get update && sudo apt-get --reinstall install -qq language-pack-en
