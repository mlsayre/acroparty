# language setting
language: ruby

# version number
rvm:
 - 2.1.0

before_script:
 - psql -c 'create database acroparty_development;' -U matt -w
 - psql -c 'create database acroparty_test;' -U matt -w
